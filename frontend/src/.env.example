# Nuxt.js Frontend Environment Variables Configuration

# API Configuration
# バックエンドAPIのベースURL（パブリック - クライアントサイドでも使用）
NUXT_PUBLIC_API_BASE_URL=http://localhost:80

# OAuth2 Configuration
# Rails APIから発行されたOAuth2クライアント認証情報（パブリック）
NUXT_PUBLIC_OAUTH_CLIENT_ID=
NUXT_PUBLIC_OAUTH_CLIENT_SECRET=

# Development Server Configuration
# Nuxt開発サーバーの設定（nuxt.config.ts内で設定済み）
# HOST=0.0.0.0
# PORT=3000

# Build Configuration
# NODE_ENV=development

# Runtime Configuration (Private - Server-side only)
# サーバーサイドでのAPIアクセス用（コンテナ間通信）
# NUXT_API_BASE_URL=http://backend:3000